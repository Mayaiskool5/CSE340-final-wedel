<%- include('../partials/header') %>

<link rel="stylesheet" href="/css/vehicle-catalog.css">
<link rel="stylesheet" href="/css/main.css">


<main>
    <div class="vehicle-header">
        <img src="<%= vehicle.main_image || '/images/no-image.jpg' %>" alt="<%= vehicle.make %> <%= vehicle.model %>" class="vehicle-image">
        <h1><%= vehicle.make %> <%= vehicle.model %> (<%= vehicle.year %>)</h1>
        <p class="vehicle-meta">
            <strong>Category:</strong> <%= vehicle.category_name || 'N/A' %> |
            <strong>Owner:</strong> <%= vehicle.owner_name %> |
            <strong>Price:</strong> <%= vehicle.price ? ('$' + vehicle.price) : 'Contact for price' %>
        </p>
        <p class="vehicle-description"><%= vehicle.description || 'No description available.' %></p>
        <% if (vehicle.specs) { %>
            <div class="vehicle-specs">
                <h3>Specifications</h3>
                <pre><%= JSON.stringify(vehicle.specs, null, 2) %></pre>
            </div>
        <% } %>
    </div>


    <section class="add-review">
        <hr>
        <h3>Leave a Review</h3>
        <% if (locals.user) { %>
            <form action="/reviews/add" method="POST" class="review-form">
                <input type="hidden" name="vehicle_id" value="<%= vehicle.id %>">
                <input type="hidden" name="vehicle_slug" value="<%= vehicle.slug %>">
                <div class="form-group">
                    <label for="review_rating">Rating (1-5):</label>
                    <select name="review_rating" id="review_rating" required>
                        <option value="5">5 Stars - Excellent</option>
                        <option value="4">4 Stars - Very Good</option>
                        <option value="3">3 Stars - Good</option>
                        <option value="2">2 Stars - Fair</option>
                        <option value="1">1 Star - Poor</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="review_text">Your Review:</label>
                    <textarea name="review_text" id="review_text" rows="4" placeholder="Share your experience with this <%= vehicle.make %>..." required></textarea>
                </div>
                <button type="submit" class="btn-primary">Submit Review</button>
            </form>
        <% } else { %>
            <div class="login-notice">
                <p>You must be <a href="/login">logged in</a> to leave a review.</p>
            </div>
        <% } %>
    </section>

    <div class="navigation">
        <p><a href="/catalog">&larr; Back to Vehicle Catalog</a></p>
    </div>
</main>
<%- include('../partials/footer') %>